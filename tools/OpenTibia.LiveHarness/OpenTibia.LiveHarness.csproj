<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net472</TargetFramework>
        <LangVersion>latest</LangVersion>
        <Nullable>enable</Nullable>
        <IsPackable>false</IsPackable>
        <IsTestProject>false</IsTestProject>
        <AssemblyName>OpenTibia.LiveHarness</AssemblyName>
        <RootNamespace>OpenTibia.LiveHarness</RootNamespace>
        <!-- Deliberately outside PowerAutomate.Desktop.slnx so it cannot affect the coverage gate. -->
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="..\..\modules\OpenTibia.Client\OpenTibia.Client.csproj" />
        <ProjectReference Include="..\..\modules\OpenTibia.Protocol\OpenTibia.Protocol.csproj" />
    </ItemGroup>

</Project>
